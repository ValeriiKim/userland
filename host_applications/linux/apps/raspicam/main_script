#!/bin/bash
# Маленький скрипт, который должен запускаться сразу после включения расберри.
# После того, как пользователь выключит цифровой выход в терминале CAN-WAY
# программа farvcam должна закрыться и расберри выключится
sudo ./farvcam
sudo shutdown -h now